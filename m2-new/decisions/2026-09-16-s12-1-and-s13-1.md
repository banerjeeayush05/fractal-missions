# Owner decisions — 2026-09-16

Recorded verbatim, then interpreted. Findings are in `OPEN_QUESTIONS.md`.

## Verbatim

> 1c, 2 accept

Given in reply to the options presented for S12.1 (option 1c) and S13.1 (item 2).

## Interpretation

**S12.1 — cumulative reinitialisation drift: option c.** Record as a known discrepancy. Do not change
reinitialisation now. Re-measure V1 (full travel), V9 and V14a after WENO5 is built at stage 14, and
revisit the decision with those numbers.

- V1, V9 and V14a stay `xfail(strict=True)`; their reasons now cite this decision instead of
  "pending".
- Tolerances are unchanged.
- Not decided by this answer: the side question of adding a check that bounds drift accumulated over
  a full run. It remains open.
- Not decided by this answer: whether to first compare against the original `m2/` tree (option a).
  Option c does not require it.

**S13.1 — tolerance for V14a, V14b, V14c: accepted.** 1 % relative error for all three, on the rule
recorded in S13.1. No longer provisional.

## Not yet done

The repo rule is that a decision amends the PRD in the same commit that implements it. The PRD lives
at `m2/M2_PRD.md`, in the original tree, which this rebuild does not modify. Whether these decisions
should amend that file, or stay local to `m2-new`, is itself an owner question and has not been acted
on.
